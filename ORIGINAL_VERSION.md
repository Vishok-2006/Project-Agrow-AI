# 🌐 Original HTML Version

The original HTML version of Agrow AI is currently being served.

## 🚀 How to Run

Since this is a static HTML file, you can serve it using any simple HTTP server.

### Using Python (Recommended)
```bash
python3 -m http.server 8000
```
Access at: `http://localhost:8000`

### Using Node.js (http-server)
```bash
npx http-server -p 8000
```

## 📁 Files
- `index.html` - The original HTML file (restored from backup)
- `index.html.react` - The React entry point (renamed from index.html)

## 🔄 Switching Back to React
To switch back to the React version:
1. Rename `index.html` to `index.html.original`
2. Rename `index.html.react` to `index.html`
3. Run `npm run dev`
